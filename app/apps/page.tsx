'use client';
import {useMemo,useState} from 'react';
import Link from 'next/link';
import {Search} from 'lucide-react';
import {SiteHeader} from '@/components/site-header';
import {ecosystemApps,ecosystemCategories} from '@/lib/ecosystem-apps';

export default function AppsPage(){const [query,setQuery]=useState('');const [category,setCategory]=useState('All');const filtered=useMemo(()=>ecosystemApps.filter(app=>(category==='All'||app.category===category)&&`${app.name} ${app.category} ${app.description}`.toLowerCase().includes(query.toLowerCase())),[query,category]);return <div className="directory-page"><SiteHeader/><main><section className="page-hero"><div className="section-kicker">MONAD APP DIRECTORY</div><h1>Explore the ecosystem.<br/><span>Talk to it through MOT.</span></h1><p>Every app listed here is part of the wider Monad landscape. Integration labels show what MOT can actually do today.</p></section><section className="directory-tools"><label><Search size={18}/><input value={query} onChange={event=>setQuery(event.target.value)} placeholder="Search apps, categories or use cases…"/></label><div>{ecosystemCategories.map(item=><button className={category===item?'active':''} onClick={()=>setCategory(item)} key={item}>{item}</button>)}</div></section><section className="directory-grid">{filtered.map(app=><article key={app.name}><div className="app-monogram">{app.name.slice(0,2).toUpperCase()}</div><span className={`app-state ${app.status}`}>{app.status==='integrated'?'Integrated':app.status==='next'?'Next integration':'Discover'}</span><h2>{app.name}</h2><small>{app.category}</small><p>{app.description}</p>{app.status==='integrated'?<Link href="/chat">Use with MOT →</Link>:<span className="directory-note">Integration not active yet</span>}</article>)}</section>{!filtered.length&&<p className="no-results">No apps match that search.</p>}</main></div>}
