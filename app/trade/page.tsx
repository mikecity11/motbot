import Home from '@/app/page';

export const metadata = {
  title: 'Trade with MOTBOT | MOTBOT on Monad',
  description: 'The MOTBOT conversational trading workspace.',
};

export default function TradePage() {
  return <Home />;
}
